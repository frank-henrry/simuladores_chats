# Copiar a .env y completar. Este .env representa la config que tendria el
# BACKEND (PHP) del portal externo (no el navegador del cliente).

# URL que usa ESTE backend para llamar al Core API. Con `docker compose up`
# (une este contenedor a la red de virtualfact_chats) usar el nombre del
# servicio: http://core-api:3002/api/v1
CORE_API_INTERNAL_URL=http://core-api:3002/api/v1

# URLs que se le devuelven al NAVEGADOR (siempre corre en el host, nunca
# dentro de Docker) para que llame directo al Core API y al Hub. SIEMPRE
# deben ser localhost (o el dominio público real en producción), nunca el
# nombre del contenedor.
CORE_API_PUBLIC_URL=http://localhost:3002/api/v1
WS_PUBLIC_URL=http://localhost:3001

# La llave estática que el equipo de virtualfact_chats le entrega a este
# equipo por un canal seguro (nunca por correo/chat en texto plano). No es un
# JWT, no se firma nada -- debe coincidir EXACTAMENTE con PORTAL_API_KEY del
# .env de virtualfact_chats/servicio-core-api/.env
PORTAL_API_KEY=REEMPLAZAR
